# 2026-01-29 - Researcher Daily Notes

## Conversa sobre Sistema de Memória

Markus perguntou sobre como funciona o sistema de memória do Moltbot:

### Descobertas:
- O sistema usa SQLite + embeddings (OpenAI text-embedding-3-small)
- Busca híbrida: BM25 (keywords) + vetores (semântico)
- Arquivos indexados: `memory/*.md` + `MEMORY.md`
- Sessions podem ser indexadas com `sessionMemory: true`

### Problema encontrado:
- A pasta `memory/` do researcher foi deletada entre sessões
- O projeto do monitor foi movido para o agente `moltbot`

### Comparativo de agentes:
- `opsec`: indexando corretamente 8 arquivos de memória
- `moltbot`: só sessions
- `researcher`: pasta memory/ deletada
